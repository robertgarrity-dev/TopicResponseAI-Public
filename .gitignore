# 🚀 Node.js Dependencies  
node_modules/
package-lock.json

# 🚀 Build Output 
dist/
build/
coverage/
server/public/

# 🚀 Environment Variables (Security - DO NOT PUSH!)
.env
.env.local
.env.* # Ignore all environment-specific .env files

# 🚀 Replit-Specific Files (Keep only on Replit)
.replit
replit.nix
.upm/
.cache/
.local/

# 🚀 Database & ORM Configuration (Prevent DB Leaks)
drizzle.config.ts # Drizzle ORM config (if not needed in public repo)
server/db.ts # Contains database setup, prevent accidental exposure
server/storage.ts # Contains DB storage logic

# 🚀 IDE and System Files (Mac, VSCode, Replit, etc.)
.DS_Store
.vscode/
*.swp # Vim swap files

# 🚀 Logs and Debugging Files
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
debug.log

# 🚀 Temporary and Generated Files
*.tar.gz
*.zip
tmp/
temp/
.vite/

# 🚀 TypeScript & Vite Specific Files
vite.config.ts.*
tsconfig.tsbuildinfo

# 🚀 Authentication & Secrets Files (Prevent Exposing API Keys)
server/auth.ts # Authentication handling (not needed in public repo)
server/gemini.ts # AI API Key handling (not needed in public repo)
server/index.ts # Avoid accidental exposure of session secrets

# 🚀 Replit Backup Files (Prevent from being pushed)
backup/
replit.db
